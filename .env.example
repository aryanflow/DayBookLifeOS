# Copy to `.env` for local dev, or set in Netlify / Vercel dashboard.
# Sync is optional - the app works without it.

# URL of your deployed Cloudflare Worker (no trailing slash)
# Example: https://daybook-sync.your-subdomain.workers.dev
VITE_SYNC_API_URL=
